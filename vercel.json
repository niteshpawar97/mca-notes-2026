{
  "buildCommand": "cd notes-app && npm install && npm run build",
  "outputDirectory": "notes-app/dist",
  "framework": "vite",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
